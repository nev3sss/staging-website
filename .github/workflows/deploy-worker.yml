name: Deploy — Worker (API)

on:
  push:
    branches: [main]
    paths: ["workers/**", ".github/workflows/deploy-worker.yml"]
  workflow_dispatch:
    inputs:
      env:
        description: "Target environment"
        required: true
        default: "production"
        type: choice
        options: ["staging", "production"]

# Least-privilege permissions for Worker deploy.
permissions:
  contents: read
  deployments: write

jobs:
  deploy-worker:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: workers

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: workers/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Deploy Worker
        run: |
          if [[ "${{ github.event.inputs.env || 'production' }}" == "staging" ]]; then
            npx wrangler deploy --env staging
          else
            npx wrangler deploy --env production
          fi
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}

      # D1 migrations run only on production workflow_dispatch.
      # NOT on every push — migrations are irreversible.
      - name: Run D1 migrations (production only)
        if: github.event.inputs.env == '' || github.event.inputs.env == 'production'
        run: npx wrangler d1 migrations apply nev3s-dealership-db --remote
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
